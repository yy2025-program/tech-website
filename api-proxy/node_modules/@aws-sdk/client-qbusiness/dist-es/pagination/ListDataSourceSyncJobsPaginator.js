import { createPaginator } from "@smithy/core";
import { ListDataSourceSyncJobsCommand, } from "../commands/ListDataSourceSyncJobsCommand";
import { QBusinessClient } from "../QBusinessClient";
export const paginateListDataSourceSyncJobs = createPaginator(QBusinessClient, ListDataSourceSyncJobsCommand, "nextToken", "nextToken", "maxResults");
