import { createPaginator } from "@smithy/core";
import { ListWebExperiencesCommand, } from "../commands/ListWebExperiencesCommand";
import { QBusinessClient } from "../QBusinessClient";
export const paginateListWebExperiences = createPaginator(QBusinessClient, ListWebExperiencesCommand, "nextToken", "nextToken", "maxResults");
